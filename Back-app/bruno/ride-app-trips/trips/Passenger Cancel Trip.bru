meta {
  name: Passenger Cancel Trip
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v1/trips/{{tripId}}/cancel/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{passengerAccessToken}}
}
