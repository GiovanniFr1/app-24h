meta {
  name: Driver Register Missing Docs (Should Fail)
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/users/register/
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{driverMissingDocsEmail}}",
    "password": "{{driverPassword}}",
    "full_name": "Driver Missing Docs",
    "role": "DRIVER",
    "cpf": "{{driverCpf}}",
    "cnh": "{{driverCnh}}"
  }
}
