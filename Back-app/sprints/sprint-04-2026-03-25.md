# Sprint 04 - 2026-03-25

## Objetivo da Sprint
Migração do backend de Django para Firebase. Setup completo da infraestrutura Firebase (Firestore, Auth, Cloud Functions, Storage) e configuração do Flutter para usar Firebase SDK.

## Itens Implementados

### Firebase Backend (`firebase/`)
- Estrutura do projeto Firebase criada (`firebase.json`, `firestore.indexes.json`)
- Regras Firestore com controle de acesso por role (`firestore.rules`)
- Regras Storage com limite de tamanho e tipo (`storage.rules`)
- Cloud Functions em TypeScript (Node.js 20):
  - `auth.ts`: `onUserCreated` (trigger), `setRole` (callable), `saveFcmToken` (callable)
  - `admin.ts`: `approveDriver`, `rejectDriver`, `setAdminRole` (callables — apenas admins)
  - `index.ts`: entry point com exports organizados por sprint

### Flutter (`app-acre-24h-front/`)
- Firebase packages adicionados ao `pubspec.yaml`:
  - `firebase_core`, `firebase_auth`, `cloud_firestore`, `firebase_messaging`, `firebase_storage`
- `main.dart` atualizado com `Firebase.initializeApp()` e `WidgetsFlutterBinding.ensureInitialized()`
- `firebase_options.dart` placeholder criado (será substituído pelo `flutterfire configure`)
- Android: Google Services plugin adicionado em `settings.gradle.kts` e `app/build.gradle.kts`

## Arquivos Principais Alterados
- `firebase/firebase.json` (novo) — movido para dentro de app-acre-24h-back/
- `firebase/firestore.rules` (novo)
- `firebase/storage.rules` (novo)
- `firebase/firestore.indexes.json` (novo)
- `firebase/functions/package.json` (novo)
- `firebase/functions/tsconfig.json` (novo)
- `firebase/functions/src/index.ts` (novo)
- `firebase/functions/src/auth.ts` (novo)
- `firebase/functions/src/admin.ts` (novo)
- `AGENTS.md` — reescrito para stack Firebase (Django removido)
- `app-acre-24h-front/pubspec.yaml`
- `app-acre-24h-front/lib/main.dart`
- `app-acre-24h-front/lib/firebase_options.dart` (placeholder)
- `app-acre-24h-front/android/settings.gradle.kts`
- `app-acre-24h-front/android/app/build.gradle.kts`

## Passos Manuais Necessários (pelo desenvolvedor)

1. Criar projeto no Firebase Console (console.firebase.google.com)
2. Adicionar app Android (package: `com.example.app_acre_24h_front`) → baixar `google-services.json` → colocar em `android/app/`
3. Adicionar app iOS → baixar `GoogleService-Info.plist` → colocar em `ios/Runner/`
4. Ativar Authentication → Email/Password
5. Criar Firestore Database (modo teste)
6. Criar Storage bucket (modo teste)
7. Rodar na pasta `app-acre-24h-front/`:
   ```bash
   dart pub global activate flutterfire_cli
   flutterfire configure
   flutter pub get
   ```
8. Rodar na pasta `firebase/functions/`:
   ```bash
   npm install
   npm run build
   ```
9. Criar primeiro admin manualmente (ver seção abaixo)

## Como criar o primeiro admin
Após deploy das functions, rodar este script uma única vez:
```bash
# Na pasta firebase/
firebase functions:shell
# Dentro do shell:
admin.auth().setCustomUserClaims('UID_DO_ADMIN', { role: 'admin' })
```
Ou via Firebase Console → Authentication → selecionar usuário → Custom Claims.

## Pendências e Próximos Passos
- Sprint 05: Admin Web App (Next.js) — driver approval backoffice
- Sprint 06: Firebase Auth no Flutter — substituir JWT/Django auth
- Sprint 07: Trip lifecycle em Cloud Functions
- Sprint 08: FCM push notifications
- Sprint 09: Pagar.me payment
